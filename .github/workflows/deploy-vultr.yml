name: 🚀 Deploy to Vultr VPS (Zero Downtime)

on:
  push:
    branches: [ "feature/migration" ]
  workflow_dispatch:  # Permet le déploiement manuel

env:
  APP_PATH: /var/www/axontis
  RELEASES_PATH: /var/www/axontis/releases
  PHP_VERSION: '8.3'
  NODE_VERSION: '22'

jobs:
  # =============================================================================
  # Job 0: Test Migrations (SQLite)
  # =============================================================================
  test-migrations:
    name: 🧪 Test Migrations
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐘 Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: mbstring, intl, zip, bcmath, gd, pdo, pdo_sqlite, sqlite3
          coverage: none

      - name: 📦 Install Composer dependencies
        run: composer install --prefer-dist --no-interaction

      - name: 🗄️ Test migrations on MySQL (Docker)
        run: |
          echo "🧪 Testing migrations on MySQL with Docker..."
          echo ""

          # Démarrer un conteneur MySQL temporaire
          echo "🐳 Starting MySQL container..."
          docker run -d \
            --name mysql-test \
            -e MYSQL_ROOT_PASSWORD=testpassword \
            -e MYSQL_DATABASE=axontis_test \
            -p 33306:3306 \
            mysql:8.0 \
            --default-authentication-plugin=mysql_native_password

          # Attendre que MySQL soit complètement initialisé
          echo "⏳ Waiting for MySQL to be ready..."
          sleep 5  # Attendre que le conteneur démarre

          for i in {1..30}; do
            # Vérifier si MySQL accepte les connexions
            if docker exec mysql-test mysql -uroot -ptestpassword -e "SELECT 1;" &>/dev/null; then
              echo "✅ MySQL is ready!"
              break
            fi
            echo "   Attempt $i/30..."
            sleep 3
          done

          # Vérifier la connexion finale
          if ! docker exec mysql-test mysql -uroot -ptestpassword -e "SHOW DATABASES;" 2>/dev/null; then
            echo "❌ MySQL connection failed!"
            echo "Container logs:"
            docker logs mysql-test 2>&1 | tail -20
            docker stop mysql-test
            docker rm mysql-test
            exit 1
          fi

          echo "✅ MySQL connection verified"

          echo ""
          echo "📥 Importing existing MySQL schema..."

          # Importer le schéma existant depuis database/schema/mysql-schema.sql
          if [ -f "database/schema/mysql-schema.sql" ]; then
            docker exec -i mysql-test mysql -uroot -ptestpassword axontis_test < database/schema/mysql-schema.sql

            if [ $? -eq 0 ]; then
              echo "✅ Schema imported successfully"
            else
              echo "⚠️  Schema import had warnings (this is normal for conditional statements)"
            fi
          else
            echo "⚠️  No existing schema found, starting from scratch"
          fi

          echo ""
          echo "📋 Tables in database before migrations:"
          docker exec mysql-test mysql -uroot -ptestpassword axontis_test -e "SHOW TABLES;"

          # Configurer l'environnement de test
          cp .env.example .env.testing
          php artisan key:generate --env=testing

          # Configurer MySQL dans .env.testing
          cat >> .env.testing << EOF

          DB_CONNECTION=mysql
          DB_HOST=127.0.0.1
          DB_PORT=33306
          DB_DATABASE=axontis_test
          DB_USERNAME=root
          DB_PASSWORD=testpassword
          EOF

          echo ""
          echo "🗄️ Running pending migrations..."

          # Marquer les migrations existantes comme déjà exécutées
          # (car elles sont dans le schéma importé)
          php artisan migrate:install --env=testing

          # Obtenir la liste des migrations déjà dans le schéma
          # et les marquer comme exécutées
          echo "Marking existing migrations as run..."
          php artisan tinker --env=testing --execute="
            // Récupérer toutes les migrations
            \$migrator = app('migrator');
            \$files = \$migrator->getMigrationFiles(\$migrator->paths());

            // Marquer uniquement les anciennes migrations (avant 2024_01_15)
            foreach (\$files as \$name => \$path) {
              if (strpos(\$name, '2024_01_15') === false) {
                DB::table('migrations')->insertOrIgnore([
                  'migration' => \$name,
                  'batch' => 1
                ]);
                echo \"Marked: \$name\n\";
              }
            }
          " 2>/dev/null || echo "Could not mark old migrations"

          echo ""
          echo "Running NEW migrations only..."
          php artisan migrate --env=testing --force --no-interaction

          if [ $? -eq 0 ]; then
            echo "✅ Migrations executed successfully"
          else
            echo "❌ Migrations failed!"
            docker logs mysql-test
            docker stop mysql-test
            docker rm mysql-test
            exit 1
          fi

          # Tester le rollback des nouvelles migrations uniquement
          echo ""
          echo "Testing rollback of new migrations..."
          php artisan migrate:rollback --env=testing --force --no-interaction --step=1

          if [ $? -eq 0 ]; then
            echo "✅ Rollback successful"
          else
            echo "❌ Rollback failed!"
            docker stop mysql-test
            docker rm mysql-test
            exit 1
          fi

          # Re-tester les migrations (pour vérifier l'idempotence)
          echo ""
          echo "Re-testing migrations (idempotence check)..."
          php artisan migrate --env=testing --force --no-interaction

          if [ $? -eq 0 ]; then
            echo "✅ Migrations are idempotent"
          else
            echo "❌ Migrations failed on second run!"
            docker stop mysql-test
            docker rm mysql-test
            exit 1
          fi

          # Afficher le schéma final
          echo ""
          echo "📋 Final database schema:"
          docker exec mysql-test mysql -uroot -ptestpassword axontis_test -e "SHOW TABLES;"

          echo ""
          echo "📊 Checking critical tables:"
          docker exec mysql-test mysql -uroot -ptestpassword axontis_test -e "DESCRIBE communications;" || echo "⚠️  communications table check"
          docker exec mysql-test mysql -uroot -ptestpassword axontis_test -e "DESCRIBE notification_preferences;" || echo "⚠️  notification_preferences table check"

          # Nettoyer
          echo ""
          echo "🧹 Cleaning up..."
          docker stop mysql-test
          docker rm mysql-test

          echo ""
          echo "🎉 All migration tests passed!"

  # =============================================================================
  # Job 1: Build & Deploy (Zero Downtime)
  # =============================================================================
  deploy:
    name: 🚀 Build & Deploy to Vultr (Zero Downtime)
    runs-on: ubuntu-latest
    needs: test-migrations  # Ne déployer que si les tests passent

    steps:
      # =========================================================================
      # 1. CHECKOUT & SETUP
      # =========================================================================
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🐘 Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: mbstring, intl, zip, bcmath, gd, pdo, pdo_mysql, fileinfo
          coverage: none

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      # =========================================================================
      # 2. BUILD
      # =========================================================================
      - name: 🔧 Install Composer dependencies
        run: composer install --no-dev --optimize-autoloader --prefer-dist

      - name: 🔧 Install Node dependencies & Build assets
        run: |
          npm ci
          npm run build

      # =========================================================================
      # 3. CREATE RELEASE ARCHIVE
      # =========================================================================
      - name: 🗂️ Create release archive
        run: |
          set +e
          tar -czf release.tar.gz \
            --exclude='release.tar.gz' \
            --exclude='node_modules' \
            --exclude='tests' \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='.env' \
            --exclude='.env.example' \
            --exclude='.env.production.example' \
            --exclude='storage/logs/*' \
            --exclude='storage/framework/cache/data/*' \
            --exclude='storage/framework/sessions/*' \
            --exclude='storage/framework/views/*' \
            --exclude='resources/*' \
            .
          TAR_EXIT=$?
          set -e
          if [ $TAR_EXIT -ge 2 ]; then
            echo "❌ tar failed with exit code $TAR_EXIT"
            exit $TAR_EXIT
          fi
          echo "✅ Archive created successfully"

      # =========================================================================
      # 4. SSH SETUP
      # =========================================================================
      - name: 🔐 Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VULTR_SSH_PRIVATE_KEY }}

      - name: 📋 Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.VULTR_HOST }} >> ~/.ssh/known_hosts

      - name: 🔍 Test SSH connection
        run: |
          echo "Testing SSH connection to ${{ secrets.VULTR_USER }}@${{ secrets.VULTR_HOST }}..."
          ssh -o BatchMode=yes -o ConnectTimeout=10 -v ${{ secrets.VULTR_USER }}@${{ secrets.VULTR_HOST }} "echo 'SSH connection successful'" || {
            echo "❌ SSH connection failed!"
            echo ""
            echo "Please ensure following public key is added to ~/.ssh/authorized_keys on your Vultr server:"
            ssh-add -L
            exit 1
          }

      # =========================================================================
      # 5. UPLOAD TO SERVER
      # =========================================================================
      - name: 📤 Upload release to server
        run: |
          scp release.tar.gz ${{ secrets.VULTR_USER }}@${{ secrets.VULTR_HOST }}:/tmp/release.tar.gz

      # =========================================================================
      # 6. ZERO DOWNTIME DEPLOYMENT
      # =========================================================================
      - name: 🚀 Deploy on server (Zero Downtime)
        env:
          # Database
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
          DB_DATABASE: ${{ secrets.DB_DATABASE }}
          DB_USERNAME: ${{ secrets.DB_USERNAME }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          # App
          APP_KEY: ${{ secrets.APP_KEY }}
          APP_URL: ${{ secrets.APP_URL }}
          # Cloudflare R2
          CLOUDFLARE_R2_ACCESS_KEY_ID: ${{ secrets.CLOUDFLARE_R2_ACCESS_KEY_ID }}
          CLOUDFLARE_R2_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_SECRET_ACCESS_KEY }}
          CLOUDFLARE_R2_BUCKET: ${{ secrets.CLOUDFLARE_R2_BUCKET }}
          CLOUDFLARE_R2_ENDPOINT: ${{ secrets.CLOUDFLARE_R2_ENDPOINT }}
          CLOUDFLARE_R2_URL: ${{ secrets.CLOUDFLARE_R2_URL }}
          # DocuSign
          DOCUSIGN_CLIENT_ID: ${{ secrets.DOCUSIGN_CLIENT_ID }}
          DOCUSIGN_USER_ID: ${{ secrets.DOCUSIGN_USER_ID }}
          DOCUSIGN_ACCOUNT_ID: ${{ secrets.DOCUSIGN_ACCOUNT_ID }}
          DOCUSIGN_BASE_PATH: ${{ secrets.DOCUSIGN_BASE_PATH }}
          DOCUSIGN_OAUTH_BASE_PATH: ${{ secrets.DOCUSIGN_OAUTH_BASE_PATH }}
          DOCUSIGN_HMAC_KEY: ${{ secrets.DOCUSIGN_HMAC_KEY }}
          # Stripe
          STRIPE_PUBLIC_KEY: ${{ secrets.STRIPE_PUBLIC_KEY }}
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
          STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
          # Sentry
          SENTRY_LARAVEL_DSN: ${{ secrets.SENTRY_LARAVEL_DSN }}
          # Redis
          REDIS_PASSWORD: ${{ secrets.REDIS_PASSWORD }}
          # Mail
          MAIL_MAILER: ${{ secrets.MAIL_MAILER }}
          MAIL_HOST: ${{ secrets.MAIL_HOST }}
          MAIL_PORT: ${{ secrets.MAIL_PORT }}
          MAIL_USERNAME: ${{ secrets.MAIL_USERNAME }}
          MAIL_PASSWORD: ${{ secrets.MAIL_PASSWORD }}
          MAIL_FROM_ADDRESS: ${{ secrets.MAIL_FROM_ADDRESS }}
          MAIL_FROM_NAME: ${{ secrets.MAIL_FROM_NAME }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          MAILGUN_DOMAIN: ${{ secrets.MAILGUN_DOMAIN }}
          MAILGUN_SECRET: ${{ secrets.MAILGUN_SECRET }}
          MAILGUN_ENDPOINT: ${{ secrets.MAILGUN_ENDPOINT }}
          BREVO_SMTP_HOST: ${{ secrets.BREVO_SMTP_HOST }}
          BREVO_SMTP_PORT: ${{ secrets.BREVO_SMTP_PORT }}
          BREVO_SMTP_USERNAME: ${{ secrets.BREVO_SMTP_USERNAME }}
          BREVO_API_KEY: ${{ secrets.BREVO_API_KEY }}
          # SMS
          TWILIO_SID: ${{ secrets.TWILIO_SID }}
          TWILIO_TOKEN: ${{ secrets.TWILIO_TOKEN }}
          TWILIO_FROM: ${{ secrets.TWILIO_FROM }}
          TWILIO_WHATSAPP_FROM: ${{ secrets.TWILIO_WHATSAPP_FROM }}
          # Messaging
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          NOTIFICATION_SLACK_WEBHOOK: ${{ secrets.NOTIFICATION_SLACK_WEBHOOK }}
          NOTIFICATION_TELEGRAM_CHAT_ID: ${{ secrets.NOTIFICATION_TELEGRAM_CHAT_ID }}
        run: |
          ssh ${{ secrets.VULTR_USER }}@${{ secrets.VULTR_HOST }} \
          "export DB_HOST='$DB_HOST' \
          DB_PORT='$DB_PORT' \
          DB_DATABASE='$DB_DATABASE' \
          DB_USERNAME='$DB_USERNAME' \
          DB_PASSWORD='$DB_PASSWORD' \
          APP_KEY='$APP_KEY' \
          APP_URL='$APP_URL' \
          CLOUDFLARE_R2_ACCESS_KEY_ID='$CLOUDFLARE_R2_ACCESS_KEY_ID' \
          CLOUDFLARE_R2_SECRET_ACCESS_KEY='$CLOUDFLARE_R2_SECRET_ACCESS_KEY' \
          CLOUDFLARE_R2_BUCKET='$CLOUDFLARE_R2_BUCKET' \
          CLOUDFLARE_R2_ENDPOINT='$CLOUDFLARE_R2_ENDPOINT' \
          CLOUDFLARE_R2_URL='$CLOUDFLARE_R2_URL' \
          DOCUSIGN_CLIENT_ID='$DOCUSIGN_CLIENT_ID' \
          DOCUSIGN_USER_ID='$DOCUSIGN_USER_ID' \
          DOCUSIGN_ACCOUNT_ID='$DOCUSIGN_ACCOUNT_ID' \
          DOCUSIGN_BASE_PATH='$DOCUSIGN_BASE_PATH' \
          DOCUSIGN_OAUTH_BASE_PATH='$DOCUSIGN_OAUTH_BASE_PATH' \
          DOCUSIGN_HMAC_KEY='$DOCUSIGN_HMAC_KEY' \
          STRIPE_PUBLIC_KEY='$STRIPE_PUBLIC_KEY' \
          STRIPE_SECRET_KEY='$STRIPE_SECRET_KEY' \
          STRIPE_WEBHOOK_SECRET='$STRIPE_WEBHOOK_SECRET' \
          SENTRY_LARAVEL_DSN='$SENTRY_LARAVEL_DSN' \
          REDIS_PASSWORD='$REDIS_PASSWORD' \
          MAIL_MAILER='$MAIL_MAILER' \
          MAIL_HOST='$MAIL_HOST' \
          MAIL_PORT='$MAIL_PORT' \
          MAIL_USERNAME='$MAIL_USERNAME' \
          MAIL_PASSWORD='$MAIL_PASSWORD' \
          MAIL_FROM_ADDRESS='$MAIL_FROM_ADDRESS' \
          MAIL_FROM_NAME='$MAIL_FROM_NAME' \
          RESEND_API_KEY='$RESEND_API_KEY' \
          MAILGUN_DOMAIN='$MAILGUN_DOMAIN' \
          MAILGUN_SECRET='$MAILGUN_SECRET' \
          MAILGUN_ENDPOINT='$MAILGUN_ENDPOINT' \
          BREVO_SMTP_HOST='$BREVO_SMTP_HOST' \
          BREVO_SMTP_PORT='$BREVO_SMTP_PORT' \
          BREVO_SMTP_USERNAME='$BREVO_SMTP_USERNAME' \
          BREVO_API_KEY='$BREVO_API_KEY' \
          TWILIO_SID='$TWILIO_SID' \
          TWILIO_TOKEN='$TWILIO_TOKEN' \
          TWILIO_FROM='$TWILIO_FROM' \
          TWILIO_WHATSAPP_FROM='$TWILIO_WHATSAPP_FROM' \
          TELEGRAM_BOT_TOKEN='$TELEGRAM_BOT_TOKEN' \
          TELEGRAM_CHAT_ID='$TELEGRAM_CHAT_ID' \
          SLACK_WEBHOOK_URL='$SLACK_WEBHOOK_URL' \
          NOTIFICATION_SLACK_WEBHOOK='$NOTIFICATION_SLACK_WEBHOOK' \
          NOTIFICATION_TELEGRAM_CHAT_ID='$NOTIFICATION_TELEGRAM_CHAT_ID'; /bin/bash" << 'DEPLOY_SCRIPT'
          set -e

          echo "=========================================="
          echo "🚀 ZERO DOWNTIME DÉPLOIEMENT AXONTIS"
          echo "📅 $(date)"
          echo "=========================================="

          APP_PATH="/var/www/axontis"
          RELEASES_PATH="$APP_PATH/releases"
          CURRENT_LINK="$APP_PATH/current"
          BACKUP_PATH="/var/backups/axontis"
          SHARED_PATH="$APP_PATH/shared"
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          NEW_RELEASE="$RELEASES_PATH/$TIMESTAMP"

          # Créer les dossiers nécessaires
          mkdir -p $RELEASES_PATH
          mkdir -p $BACKUP_PATH
          mkdir -p $SHARED_PATH/storage/{app,framework,logs}
          mkdir -p $SHARED_PATH/resources

          # ============================================
          # 1. BACKUP
          # ============================================
          echo ""
          echo "📦 Création du backup..."

          # Backup de la base de données
          if [ -f "$CURRENT_LINK/.env" ]; then
            DB_USER=$(grep -E "^DB_USERNAME=" $CURRENT_LINK/.env | cut -d'=' -f2 | tr -d '"' | tr -d "'")
            DB_PASS=$(grep -E "^DB_PASSWORD=" $CURRENT_LINK/.env | cut -d'=' -f2 | tr -d '"' | tr -d "'")
            DB_NAME=$(grep -E "^DB_DATABASE=" $CURRENT_LINK/.env | cut -d'=' -f2 | tr -d '"' | tr -d "'")
            mysqldump -u"$DB_USER" -p"$DB_PASS" "$DB_NAME" > "$BACKUP_PATH/db_${TIMESTAMP}.sql" 2>/dev/null || true
            echo "✅ Backup BDD créé"
          fi

          # Nettoyer les vieux backups
          cd $BACKUP_PATH && ls -t db_*.sql 2>/dev/null | tail -n +6 | xargs -r rm --

          # ============================================
          # 2. EXTRACTION DU NOUVEAU CODE
          # ============================================
          echo ""
          echo "📥 Extraction du nouveau code..."
          mkdir -p $NEW_RELEASE
          cd $NEW_RELEASE
          tar -xzf /tmp/release.tar.gz
          rm -f /tmp/release.tar.gz
          echo "✅ Code extrait"

          # ============================================
          # 3. SYMLINKS POUR LES FICHIERS PARTAGÉS
          # ============================================
          echo ""
          echo "🔗 Configuration des symlinks partagés..."
          mkdir -p $SHARED_PATH/storage/{app,framework/{cache,sessions,views},logs}
          rm -rf $NEW_RELEASE/storage
          rm -rf $NEW_RELEASE/resources
          ln -sf $SHARED_PATH/storage $NEW_RELEASE/storage
          ln -sf $SHARED_PATH/resources $NEW_RELEASE/resources
          echo "✅ Symlinks configurés (storage + resources)"

          # ============================================
          # 4. CONFIGURATION DU .ENV
          # ============================================
          echo ""
          echo "⚙️ Configuration de l'environnement..."

          # Copier le .env existant ou utiliser le backup
          if [ -f "$CURRENT_LINK/.env" ]; then
            cp $CURRENT_LINK/.env $NEW_RELEASE/.env
            echo "✅ .env copié depuis la version actuelle"
          else
            # Créer depuis .env.example
            if [ -f "$NEW_RELEASE/.env.example" ]; then
              cp $NEW_RELEASE/.env.example $NEW_RELEASE/.env
            else
              touch $NEW_RELEASE/.env
            fi
          fi

          # Fonction pour mettre à jour une variable .env
          update_env() {
            local key=$1
            local value=$2
            if [ -n "$value" ]; then
              if grep -q "^${key}=" $NEW_RELEASE/.env; then
                sed -i "s|^${key}=.*|${key}=${value}|" $NEW_RELEASE/.env
              else
                echo "${key}=${value}" >> $NEW_RELEASE/.env
              fi
            fi
          }

          # Mise à jour des variables avec les secrets
          update_env "APP_ENV" "production"
          update_env "APP_DEBUG" "false"
          update_env "APP_KEY" "$APP_KEY"
          update_env "APP_URL" "$APP_URL"
          update_env "DB_CONNECTION" "mysql"
          update_env "DB_HOST" "$DB_HOST"
          update_env "DB_PORT" "$DB_PORT"
          update_env "DB_DATABASE" "$DB_DATABASE"
          update_env "DB_USERNAME" "$DB_USERNAME"
          update_env "DB_PASSWORD" "$DB_PASSWORD"
          update_env "CACHE_DRIVER" "redis"
          update_env "SESSION_DRIVER" "redis"
          update_env "QUEUE_CONNECTION" "redis"
          update_env "REDIS_CLIENT" "phpredis"
          update_env "REDIS_HOST" "127.0.0.1"
          update_env "REDIS_PASSWORD" "$REDIS_PASSWORD"
          update_env "REDIS_PORT" "6379"
          update_env "REDIS_DB" "0"
          update_env "REDIS_CACHE_DB" "1"
          update_env "REDIS_SESSION_DB" "2"
          update_env "REDIS_QUEUE_DB" "3"
          update_env "CLOUDFLARE_R2_ACCESS_KEY_ID" "$CLOUDFLARE_R2_ACCESS_KEY_ID"
          update_env "CLOUDFLARE_R2_SECRET_ACCESS_KEY" "$CLOUDFLARE_R2_SECRET_ACCESS_KEY"
          update_env "CLOUDFLARE_R2_BUCKET" "$CLOUDFLARE_R2_BUCKET"
          update_env "CLOUDFLARE_R2_ENDPOINT" "$CLOUDFLARE_R2_ENDPOINT"
          update_env "CLOUDFLARE_R2_URL" "$CLOUDFLARE_R2_URL"
          update_env "DOCUSIGN_CLIENT_ID" "$DOCUSIGN_CLIENT_ID"
          update_env "DOCUSIGN_USER_ID" "$DOCUSIGN_USER_ID"
          update_env "DOCUSIGN_ACCOUNT_ID" "$DOCUSIGN_ACCOUNT_ID"
          update_env "DOCUSIGN_BASE_PATH" "$DOCUSIGN_BASE_PATH"
          update_env "DOCUSIGN_OAUTH_BASE_PATH" "$DOCUSIGN_OAUTH_BASE_PATH"
          update_env "DOCUSIGN_HMAC_KEY" "$DOCUSIGN_HMAC_KEY"
          update_env "STRIPE_PUBLIC_KEY" "$STRIPE_PUBLIC_KEY"
          update_env "STRIPE_SECRET_KEY" "$STRIPE_SECRET_KEY"
          update_env "STRIPE_WEBHOOK_SECRET" "$STRIPE_WEBHOOK_SECRET"
          update_env "SENTRY_LARAVEL_DSN" "$SENTRY_LARAVEL_DSN"
          update_env "MAIL_MAILER" "${MAIL_MAILER:-failover}"
          update_env "MAIL_HOST" "${MAIL_HOST:-localhost}"
          update_env "MAIL_PORT" "${MAIL_PORT:-25}"
          update_env "MAIL_USERNAME" "${MAIL_USERNAME:-null}"
          update_env "MAIL_PASSWORD" "${MAIL_PASSWORD:-null}"
          update_env "MAIL_FROM_ADDRESS" "${MAIL_FROM_ADDRESS:-noreply@axontis.net}"
          update_env "MAIL_FROM_NAME" "${MAIL_FROM_NAME:-Axontis}"
          update_env "RESEND_API_KEY" "$RESEND_API_KEY"
          update_env "MAILGUN_DOMAIN" "$MAILGUN_DOMAIN"
          update_env "MAILGUN_SECRET" "$MAILGUN_SECRET"
          update_env "MAILGUN_ENDPOINT" "$MAILGUN_ENDPOINT"
          update_env "BREVO_SMTP_HOST" "$BREVO_SMTP_HOST"
          update_env "BREVO_SMTP_PORT" "$BREVO_SMTP_PORT"
          update_env "BREVO_SMTP_USERNAME" "$BREVO_SMTP_USERNAME"
          update_env "BREVO_API_KEY" "$BREVO_API_KEY"
          update_env "TWILIO_SID" "$TWILIO_SID"
          update_env "TWILIO_TOKEN" "$TWILIO_TOKEN"
          update_env "TWILIO_FROM" "$TWILIO_FROM"
          update_env "TWILIO_WHATSAPP_FROM" "$TWILIO_WHATSAPP_FROM"
          update_env "TELEGRAM_BOT_TOKEN" "$TELEGRAM_BOT_TOKEN"
          update_env "TELEGRAM_CHAT_ID" "$TELEGRAM_CHAT_ID"
          update_env "SLACK_WEBHOOK_URL" "$SLACK_WEBHOOK_URL"
          update_env "NOTIFICATION_SLACK_WEBHOOK" "$NOTIFICATION_SLACK_WEBHOOK"
          update_env "NOTIFICATION_TELEGRAM_CHAT_ID" "$NOTIFICATION_TELEGRAM_CHAT_ID"

          echo "✅ .env configuré"

          # ============================================
          # 5. INSTALLATION DES DÉPENDANCES
          # ============================================
          echo ""
          echo "📦 Installation des dépendances..."
          cd $NEW_RELEASE

          # Réutiliser vendor si possible pour aller plus vite
          if [ -d "$CURRENT_LINK/vendor" ]; then
            cp -al $CURRENT_LINK/vendor $NEW_RELEASE/vendor 2>/dev/null || true
            composer install --no-dev --optimize-autoloader --no-interaction 2>/dev/null || composer install --no-dev --optimize-autoloader
          else
            composer install --no-dev --optimize-autoloader --no-interaction
          fi
          echo "✅ Dépendances installées"

          # ============================================
          # 6. OPTIMISATIONS LARAVEL (DANS LE DOSSIER TEMPORAIRE)
          # ============================================
          echo ""
          echo "⚡ Optimisation de Laravel..."

          # IMPORTANT: Nettoyer tous les caches AVANT d'optimiser
          php artisan optimize:clear

          # Publier les vues vendor mail si nécessaire
          if [ ! -d "$NEW_RELEASE/resources/views/vendor/mail" ]; then
            php artisan vendor:publish --tag=laravel-mail --force
          fi

          # Créer le storage:link
          php artisan storage:link

          # OPTIMISER - Ceci va prendre en compte toutes les variables .env
          php artisan optimize

          echo "✅ Optimisation terminée"

          # ============================================
          # 7. VÉRIFICATION DE LA NOUVELLE RELEASE
          # ============================================
          echo ""
          echo "🔍 Vérification de la nouvelle release..."

          # Test des fichiers critiques
          CRITICAL_FILES=("artisan" "public/index.php" ".env" "vendor/autoload.php")
          ALL_OK=true
          for file in "${CRITICAL_FILES[@]}"; do
            if [ ! -f "$NEW_RELEASE/$file" ]; then
              echo "❌ Fichier critique manquant: $file"
              ALL_OK=false
            fi
          done

          if [ "$ALL_OK" = false ]; then
            echo "❌ La nouvelle release n'est pas complète"
            rm -rf $NEW_RELEASE
            exit 1
          fi

          # Test de connexion BDD
          if ! php artisan tinker --execute="try { DB::connection()->getPdo(); echo 'OK'; } catch(\Exception \$e) { echo 'FAIL: '.\$e->getMessage(); }" 2>/dev/null | grep -q "OK"; then
            echo "❌ Impossible de se connecter à la base de données"
            rm -rf $NEW_RELEASE
            exit 1
          fi
          echo "✅ Connexion BDD OK"

          # ============================================
          # 8. MIGRATIONS
          # ============================================
          echo ""
          echo "🗄️ Exécution des migrations..."
          php artisan migrate --force
          echo "✅ Migrations exécutées"

          # ============================================
          # 9. CHANGEMENT ATOMIQUE DU SYMLINK (ZERO DOWNTIME)
          # ============================================
          echo ""
          echo "🔄 Switch vers la nouvelle release (ATOMIQUE)..."

          # Créer le symlink de manière atomique
          ln -sfn $NEW_RELEASE $CURRENT_LINK

          echo "✅ Symlink mis à jour"

          # ============================================
          # 10. NETTOYAGE DU CACHE ET REDÉMARRAGE SERVICES
          # ============================================
          echo ""
          echo "🔄 Nettoyage du cache et redémarrage des services..."

          # Vider Redis pour prendre en compte les changements
          if systemctl is-active --quiet redis-server; then
            if [ -n "$REDIS_PASSWORD" ] && [ "$REDIS_PASSWORD" != "null" ]; then
              redis-cli -a "$REDIS_PASSWORD" FLUSHALL --no-auth-warning >/dev/null 2>&1 || true
            else
              redis-cli FLUSHALL >/dev/null 2>&1 || true
            fi
            echo "✅ Cache Redis vidé"
          fi

          # Reload PHP-FPM pour prendre en compte les changements
          systemctl reload php8.3-fpm

          # Redémarrer les workers
          php artisan queue:restart
          supervisorctl restart axontis-worker:* 2>/dev/null || true

          echo "✅ Services redémarrés"

          # ============================================
          # 11. NETTOYAGE DES ANCIENNES RELEASES
          # ============================================
          echo ""
          echo "🧹 Nettoyage des anciennes releases..."
          cd $RELEASES_PATH
          ls -t | tail -n +4 | xargs -r rm -rf --
          echo "✅ Anciennes releases nettoyées"

          # ============================================
          # 12. PERMISSIONS
          # ============================================
          echo ""
          echo "🔒 Configuration des permissions..."
          chown -R www-data:www-data $NEW_RELEASE
          chmod -R 755 $NEW_RELEASE
          chmod -R 775 $SHARED_PATH/storage

          echo "✅ Permissions configurées"

          # ============================================
          # 13. VÉRIFICATION FINALE
          # ============================================
          echo ""
          echo "🔍 Vérification finale..."

          # Test HTTP
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost)
          if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "302" ]; then
            echo "✅ Application accessible (HTTP $HTTP_CODE)"
          else
            echo "⚠️ L'application répond avec HTTP $HTTP_CODE"
          fi

          # Vérifier les workers
          WORKER_STATUS=$(supervisorctl status axontis-worker:* 2>/dev/null | grep -c "RUNNING" || echo "0")
          if [ "$WORKER_STATUS" -gt 0 ]; then
            echo "✅ ${WORKER_STATUS} worker(s) actif(s)"
          else
            echo "⚠️ Aucun worker actif"
          fi

          echo ""
          echo "=========================================="
          echo "🎉 DÉPLOIEMENT TERMINÉ - ZERO DOWNTIME!"
          echo "📌 Release: $TIMESTAMP"
          echo "=========================================="

          DEPLOY_SCRIPT

  # =============================================================================
  # Job 2: Notify (optionnel)
  # =============================================================================
  notify:
    name: 📢 Notify
    runs-on: ubuntu-latest
    needs: deploy
    if: always()

    steps:
      - name: 📊 Deployment Status
        run: |
          if [ "${{ needs.deploy.result }}" == "success" ]; then
            echo "✅ Déploiement réussi sur Vultr VPS (Zero Downtime)"
          else
            echo "❌ Échec du déploiement"
            exit 1
          fi
